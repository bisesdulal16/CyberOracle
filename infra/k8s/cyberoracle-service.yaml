apiVersion: v1
kind: Service
metadata:
  name: cyberoracle-api-service
spec:
  type: NodePort
  selector:
    app: cyberoracle-api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080