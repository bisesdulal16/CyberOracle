server.host: "0.0.0.0"
server.name: "cyberoracle-kibana"
server.basePath: "/kibana"
server.rewriteBasePath: true
elasticsearch.hosts: ["http://localhost:9200"]

# Disable telemetry
telemetry.enabled: false
telemetry.optIn: false
