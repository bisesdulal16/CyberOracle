###############################################################################
# CyberOracle Backend — Python Dependencies
# -----------------------------------------------------------------------------
# This file lists all Python dependencies required for running, testing, and
# linting the CyberOracle FastAPI backend.
#
# To install everything:
#   pip install -r requirements.txt
###############################################################################

# -----------------------------------------------------------------------------
# 🔹 Core Application Dependencies
# -----------------------------------------------------------------------------
fastapi                       # Web framework for building the CyberOracle API
uvicorn                       # ASGI server to run FastAPI
pydantic                      # Data validation and settings management
sqlalchemy[asyncio]            # Async ORM for Postgres integration
asyncpg                        # Async Postgres driver
psycopg2-binary                # Synchronous Postgres driver (fallback)
python-dotenv                  # Loads environment variables from .env files

# -----------------------------------------------------------------------------
# 🔹 Security, Auth & Encryption
# -----------------------------------------------------------------------------
bcrypt                         # Password hashing
cryptography                   # Encryption primitives
python-jose                    # JWT creation & verification
rsa                             # RSA cryptography support
ecdsa                           # Elliptic Curve cryptography

# -----------------------------------------------------------------------------
# 🔹 Data Privacy & DLP Modules
# -----------------------------------------------------------------------------
presidio-analyzer               # Microsoft Presidio — detects sensitive data
presidio-anonymizer             # Microsoft Presidio — anonymizes sensitive data

# -----------------------------------------------------------------------------
# 🔹 Testing & Coverage Tools
# -----------------------------------------------------------------------------
pytest                          # Core testing framework
pytest-asyncio                  # Async test support for pytest
pytest-cov                      # Test coverage reporting
coverage                        # Standalone coverage reporting
httpx                           # Async HTTP client for testing FastAPI endpoints

# -----------------------------------------------------------------------------
# 🔹 Code Quality & Linting
# -----------------------------------------------------------------------------
flake8                          # Linter for enforcing code style rules
black                           # Automatic code formatter

# -----------------------------------------------------------------------------
# 🔹 Utility & Compatibility
# -----------------------------------------------------------------------------
anyio
certifi
cffi
click
exceptiongroup
h11
idna
iniconfig
packaging
passlib
pluggy
pyasn1
pycparser
Pygments
six
sniffio
starlette
tomli
typing-inspection
typing_extensions
annotated-doc
annotated-types

###############################################################################
# End of requirements.txt
# -----------------------------------------------------------------------------
# Notes:
#  • Versions removed to prevent dependency conflicts.
#  • Use `pip install -U -r requirements.txt` to fetch compatible latest versions.
#  • Optionally freeze versions after successful install using:
#        pip freeze > requirements.txt
###############################################################################
